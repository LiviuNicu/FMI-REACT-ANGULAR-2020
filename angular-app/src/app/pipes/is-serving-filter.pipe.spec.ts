import { IsServingFilterPipe } from './is-serving-filter.pipe';

describe('IsServingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IsServingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
